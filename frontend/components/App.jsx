import React from 'react';
import { Link } from 'react-router';
import MainContainer from './main/main_container';

const App = ({ children }) => (
  <div>
    <MainContainer />
  </div>
);

export default App;
